# Streamlit Cloud uses this file format for secrets.
# Locally, copy this file to .streamlit/secrets.toml and fill in real values.
# In production (Streamlit Cloud), set the same keys in the app's Settings → Secrets dashboard.

DATABASE_URL = "postgresql://postgres.PROJECT_REF:PASSWORD@aws-0-REGION.pooler.supabase.com:5432/postgres"
